mutation LoginUser($loginData: LoginUserDTO!) {
    login(loginData: $loginData) {
        id
        username
        isConfirmEmail
        token
        platforms{
            id
            platformName
            type
        }
    }
}